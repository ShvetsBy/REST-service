export declare class BoardColumns {
    id: string;
    order: number;
    title: string;
    constructor({ id, order, title, }: {
        id?: string;
        order?: number;
        title?: string;
    });
}
